local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Banchon999/BCui/refs/heads/main/Ui%20make%20money.lua"))()

local Window = OrionLib:MakeWindow({
    Name = "Your Text",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "BCuiExample",
    IntroEnabled = true,
    IntroText = "BCui Example",
    ShowIcon = false
})

local Tab = Window:MakeTab({
    Name = "Your Tab Text",
    PremiumOnly = false
})

local Section = Tab:AddSection({
    Name = "Section Text"
})

Section:AddButton({
    Name = "Button Text",
    Callback = function()
        -- code here
    end
})

Section:AddToggle({
    Name = "Enabled Toggle",
    Default = true,
    Save = true,
    Flag = "enabled_toggle",
    Callback = function(bool)
        -- code here
    end
})

Section:AddToggle({
    Name = "Disabled Toggle",
    Default = false,
    Save = true,
    Flag = "disabled_toggle",
    Callback = function(bool)
        -- code here
    end
})

Section:AddSlider({
    Name = "Slider precise",
    Min = 0,
    Max = 100,
    Default = 25,
    Increment = 1,
    ValueName = "Value",
    Callback = function(value)
        -- code here
    end
})

Section:AddSlider({
    Name = "Slider not precise",
    Min = 0,
    Max = 100,
    Default = 75,
    Increment = 5,
    ValueName = "Value",
    Callback = function(value)
        -- code here
    end
})
